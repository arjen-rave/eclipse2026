# Copy these into Render's environment variable settings for this service.
# Do not commit real values — this file documents the required variables only.

VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
PUSH_SECRET=
